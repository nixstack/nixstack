import { User } from '../model'
import { IUserIO } from '@share/io'

// export const convertUser2UserIo: (user: User) => IUserIO = (user: User) => {
//   return {
//     id: user.id,
//     name: user.name
//   }
// }

// export const convertUser2UserIoList: (userList: User[]) => IUserIO[] = (users) => users.map(item => convertUser2UserIo(item))
